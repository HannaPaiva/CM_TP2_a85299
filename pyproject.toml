[project]
name = "solitaire"
version = "0.1.0"
description = "Klondike Solitaire em Flet para o TP2 de Computacao Movel."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "duckdb",
    "flet",
    "flet-audio>=0.82.2",
]

[tool.uv]
package = false
